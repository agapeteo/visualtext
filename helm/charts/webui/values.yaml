replicaCount: 1
imagereposync:
  name: sync-repo
  repository: kupolua/sync-repo
  tag: 0.1
  pullPolicy: IfNotPresent
imagenginxlua:
  name: nginx-lua
  repository: kupolua/nginx_lua
  tag: 0.1
  pullPolicy: IfNotPresent
env:
  webuipath: "webui/html"
  gitrepourl: "https://github.com/kupolua/visualtext.git"
  gitbranch: "master"
  gitreposynctime: "10"
  webuihost: api.default.svc.cluster.local
  webuiport: "8080"
  resolver: "10.0.0.10"
service:
  name: webui-port
  type: LoadBalancer
  externalPort: 80
  internalPort: 80
strategy:
  rollingUpdate:
    maxSurge: 1
    maxUnavailable: 1
  type: RollingUpdate
volumes:
  volumename: www-data
  volumepath: /www-data
  configname: config
  configpath: /etc/nginx/conf/conf.d
restartPolicy: Always